{
  "name": "chinese-checkers-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build && npm --prefix server run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
