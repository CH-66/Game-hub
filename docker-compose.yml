services:
  game-hub:
    build:
      context: .
      dockerfile: Dockerfile
    image: game-hub:latest
    container_name: game-hub
    ports:
      - "${PORT:-4000}:4000"
    environment:
      NODE_ENV: production
      PORT: 4000
    restart: unless-stopped
