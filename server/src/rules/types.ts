import type { PieceMap, PlayerId } from '../../../shared/types.js'

export type { PieceMap, PlayerId }

export type Cube = {
  x: number
  y: number
  z: number
}

export type Axial = {
  q: number
  r: number
}
